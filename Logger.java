
//It writes information to the console. Only this class can contain calls to System.out.println()


public class Logger {

    public void log(String type, String message) { //it writes to the console in a formatted way. E.g. TimeStamp Type Message. type should be something like "INFO" or "ERROR" depending on what you want to display. Use java.util.Date for creating timestamps.
    
    }
}